/** @JSXImportSource @emotion/react **/

"use client";

import { css } from "@emotion/react";

type TimePickerProps = {
  onClose: () => void;
};

const TimePicker: React.FC<TimePickerProps> = ({ onClose }) => {
  return <div>TimePicker</div>;
};

export default TimePicker;
